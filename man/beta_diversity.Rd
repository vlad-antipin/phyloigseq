% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_diversity.R
\name{beta_diversity}
\alias{beta_diversity}
\title{Compute and Plot Beta-Diversity}
\usage{
beta_diversity(
  physeq,
  taxrank = NULL,
  fraction_id_name = NULL,
  fraction_ids = NULL,
  method = "PCoA",
  model = NULL,
  dist = "bray",
  group = NULL,
  stat = FALSE,
  comp = c(1, 2),
  ...
)
}
\arguments{
\item{physeq}{A \code{phyloseq} object.}

\item{taxrank}{Taxonomic rank to agglomerate to before ordination (e.g.
\code{"Phylum"}), via \code{\link[=tax_glom]{tax_glom()}}. Default \code{NULL} (no agglomeration).}

\item{fraction_id_name, fraction_ids}{Optionally restrict to the samples
where \code{sample_data(physeq)[[fraction_id_name]] \%in\% fraction_ids} (e.g.
selecting a single IgSeq sort fraction) before ordination. Both must be
supplied together; default \code{NULL} (no restriction).}

\item{method}{Ordination method: \code{"PCoA"}, \code{"tSNE"}, \code{"UMAP"} (distance-
based); \code{"NMDS"}, \code{"PCA"}, \code{"CA"}, \code{"DCA"} (direct on abundances); or
\code{"CCA"}, \code{"RDA"}, \code{"dbRDA"} (constrained, require \code{model}).
Case-insensitive. Default \code{"PCoA"}.}

\item{model}{For constrained methods, a string with the right-hand side
of the model formula (e.g. \code{"Var1+Var2"}). Required when \code{method} is
\code{"CCA"}/\code{"RDA"}/\code{"dbRDA"}; ignored otherwise. Default \code{NULL}.}

\item{dist}{Distance metric. Used directly for \code{method = "PCoA"}/\code{"tSNE"}/
\code{"UMAP"} (also accepts a precomputed \code{dist} object) and for
\code{method = "dbRDA"}; ignored by the other methods. Default \code{"bray"}.}

\item{group}{\code{NULL} (default) or the name of a \code{sample_data} column used
to color/group the plot and, when \code{stat = TRUE}, as the
\code{\link[=stat_beta_diversity]{stat_beta_diversity()}} test label.}

\item{stat}{Logical; run \code{\link[=stat_beta_diversity]{stat_beta_diversity()}} on \code{group} and annotate
the plot with its result. Default \code{FALSE}.}

\item{comp}{Length-2 integer vector of ordination axes to plot and (when
\code{stat = TRUE}) to test. Default \code{c(1, 2)}.}

\item{...}{Passed on to \code{\link[=plot_beta_diversity]{plot_beta_diversity()}} (e.g. \code{facet},
\code{hover.variables}, \code{ellipses}; see its documentation once written).}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Thin convenience wrapper chaining \code{\link[=get_beta_diversity]{get_beta_diversity()}}, optionally
\code{\link[=stat_beta_diversity]{stat_beta_diversity()}}, and \code{\link[=plot_beta_diversity]{plot_beta_diversity()}} in one call.
}
\examples{
data(ps_16s_refinement)
beta_diversity(ps_16s_refinement, group = "Protocol", stat = TRUE)

}
\seealso{
\code{\link[=get_beta_diversity]{get_beta_diversity()}}, \code{\link[=stat_beta_diversity]{stat_beta_diversity()}},
\code{\link[=plot_beta_diversity]{plot_beta_diversity()}}
}
