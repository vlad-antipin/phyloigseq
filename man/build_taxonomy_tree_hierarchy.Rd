% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_tree.R
\name{build_taxonomy_tree_hierarchy}
\alias{build_taxonomy_tree_hierarchy}
\title{Build a phylogenetic tree directly from the taxonomy hierarchy}
\usage{
build_taxonomy_tree_hierarchy(
  phyloseq_object,
  rank_weights = NULL,
  rank_weight_base = 1.1
)
}
\arguments{
\item{phyloseq_object}{A phyloseq object with a taxonomy table.}

\item{rank_weights}{Optional numeric vector of weights, one per rank
(including the appended taxon-name rank), highest rank first. Defaults
to \code{rank_weight_base^((number_of_ranks - 1):0)}, normalized to sum to 1.}

\item{rank_weight_base}{Base of the default exponential rank-weight
decay (see \code{\link[=get_taxonomy_tree]{get_taxonomy_tree()}} for its effect). Ignored if
\code{rank_weights} is supplied.}
}
\value{
A rooted, bifurcating object of class \code{phylo}.
}
\description{
Builds a tree directly from the nested rank structure of a taxonomy
table (Kingdom -> Phylum -> ... -> finest rank -> taxon name), without
ever forming a pairwise distance matrix. This is the scalable
alternative to \code{\link[=build_taxonomy_distance_rankwise]{build_taxonomy_distance_rankwise()}} /
\code{\link[=build_taxonomy_distance_longest_prefix]{build_taxonomy_distance_longest_prefix()}} + \code{hclust()}, which need an
n x n matrix and so are O(n^2) in both time and memory (and can
overflow \code{Matrix}'s 32-bit sparse indices for large n). This walks each
rank once to assign every taxon an integer "clade code" identifying its
lineage at that depth (vectorized paste+match, no per-row recursion or
subsetting), then builds the tree's Newick representation bottom-up
from those codes: \code{O(n * number_of_ranks)}, with no quadratic case.
}
\details{
Each rank (including the appended taxon-name rank) contributes a branch
length equal to its (normalized) weight, so the patristic (tip-to-tip
tree path) distance between two taxa equals \verb{2 *} the
\code{\link[=build_taxonomy_distance_rankwise]{build_taxonomy_distance_rankwise()}} heuristic distance between them
-- the factor of 2 is just the usual up-then-down tree-path duplication
and is irrelevant for (w)UniFrac, which normalizes by total tree length.

The result is resolved into a strictly bifurcating rooted tree (via
\code{\link[ape:collapse.singles]{ape::collapse.singles()}} then \code{\link[ape:multi2di]{ape::multi2di()}}), since
\code{phyloseq::UniFrac()} requires one and fails with an opaque error
otherwise; neither step changes any tip-to-tip patristic distance.
}
\examples{
data(ps_16s_refinement)
tree <- build_taxonomy_tree_hierarchy(ps_16s_refinement)
class(tree)
ape::is.rooted(tree)
ape::is.binary(tree)
}
