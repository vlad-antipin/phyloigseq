% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_otu_table.R
\name{incomplete_otu_table}
\alias{incomplete_otu_table}
\title{Create an incomplete OTU table with an embedded SVD factorization}
\usage{
incomplete_otu_table(
  X_inc,
  svd_fit,
  col_means = numeric(0),
  taxa_are_rows = FALSE
)
}
\arguments{
\item{X_inc}{An \code{Incomplete} object (from
\code{\link[softImpute]{Incomplete}}) with \code{dimnames} set to
\code{list(sample_names, taxa_names)}.}

\item{svd_fit}{A list with elements \code{$u}, \code{$d}, \code{$v} as
returned by \code{\link[softImpute]{softImpute}}.}

\item{col_means}{Named numeric vector of per-taxon column means used for
centering before fitting; \code{numeric(0)} if centering was not applied.}

\item{taxa_are_rows}{Logical; \code{FALSE} (default) means rows = samples,
columns = taxa, which matches the layout of the \code{Incomplete} object
built from \code{ig_coating} triplets.}
}
\value{
An \code{\link{incomplete_otu_table-class}} object.
}
\description{
Wraps a \code{softImpute::Incomplete} object and a fitted SVD factorization
into an \code{\link{incomplete_otu_table-class}}. In normal use this is
called from \code{PhyloIgSeq_to_phyloseq(imputation_method = "SVD")} rather
than directly.
}
\examples{
# 3 samples x 4 taxa, only 6 of 12 taxon-sample pairs observed
X_inc <- softImpute::Incomplete(
  i = c(1, 1, 2, 2, 3, 3),
  j = c(1, 2, 2, 3, 3, 4),
  x = c(0.5, 1.2, -0.3, 0.8, 2.1, -1.0)
)
dimnames(X_inc) <- list(paste0("S", 1:3), paste0("T", 1:4))
fit <- softImpute::softImpute(X_inc, rank.max = 2, lambda = 0.1)
ot <- incomplete_otu_table(X_inc, svd_fit = fit[c("u", "d", "v")])
dim(ot)
is.na(ot)
}
