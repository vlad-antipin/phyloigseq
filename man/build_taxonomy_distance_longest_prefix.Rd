% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_tree.R
\name{build_taxonomy_distance_longest_prefix}
\alias{build_taxonomy_distance_longest_prefix}
\title{Taxonomic distance from longest common prefix of shared ranks}
\usage{
build_taxonomy_distance_longest_prefix(
  phyloseq_object,
  rank_weights = NULL,
  rank_weight_base = 1.1
)
}
\arguments{
\item{phyloseq_object}{A phyloseq object with a taxonomy table.}

\item{rank_weights}{Optional numeric vector of weights, one per rank
(including the appended taxon-name rank), highest rank first. Defaults
to \code{rank_weight_base^((number_of_ranks - 1):0)}, normalized to sum to 1.}

\item{rank_weight_base}{Base of the default exponential rank-weight
decay (see \code{\link[=get_taxonomy_tree]{get_taxonomy_tree()}} for its effect). Ignored if
\code{rank_weights} is supplied.}
}
\value{
A \code{dist} object with one entry per taxon pair.
}
\description{
Computes a weighted taxonomy distance matrix using the
longest-common-prefix (shared ancestry) principle: two taxa are similar
if they share many consecutive taxonomic ranks from the top (Kingdom ->
Phylum -> ...), and higher ranks receive larger weights (sharing Kingdom
and Phylum but differing at Class is more informative than sharing only
Kingdom).
}
\details{
\code{distance = 1 - (shared_weight / total_weight)}, where \code{shared_weight} is
the sum of weights for the longest run of ranks shared from Kingdom
downward -- once a rank differs, all deeper ranks are treated as
unshared regardless of whether they coincidentally match. A distance of
1 means the two taxa already differ at the highest rank. Taxon names are
appended as an implicit finest rank, so distance between two different
taxa is never exactly 0 even when their annotated taxonomy is identical
-- two different taxa are known to be different sequences (otherwise
they would already have been merged upstream), so collapsing them to
distance 0 would discard that information.

Requires an n x n comparison (\verb{NOTE: very slow}), so it does not scale to
large datasets -- use \code{\link[=build_taxonomy_tree_hierarchy]{build_taxonomy_tree_hierarchy()}} instead for those.
}
\examples{
data(ps_16s_refinement)
d <- build_taxonomy_distance_longest_prefix(ps_16s_refinement)
class(d)
as.matrix(d)[1:3, 1:3]
}
