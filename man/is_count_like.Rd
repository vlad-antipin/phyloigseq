% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_count_like}
\alias{is_count_like}
\title{Check Whether Abundances Look Like Counts}
\usage{
is_count_like(x, allow_na = TRUE, consider_small_part = TRUE)
}
\arguments{
\item{x}{A numeric matrix, \code{phyloseq::otu_table}, or \link[=sparse_otu_table-class]{sparse_otu_table}.}

\item{allow_na}{Logical. If \code{TRUE} (default), \code{NA} values are ignored. If \code{FALSE},
any \code{NA} makes the function return \code{FALSE}.}

\item{consider_small_part}{Logical. If \code{TRUE} (default), only a subsample of \code{x} is
checked (the first 10000 stored values for a \link[=sparse_otu_table-class]{sparse_otu_table}, or the
top-left 100x100 corner otherwise) rather than every value, for speed on large
tables. Set \code{FALSE} to check every value.}
}
\value{
A single logical: \code{TRUE} if the checked values are all non-negative, finite
integers (\code{NA}s permitted/ignored per \code{allow_na}), \code{FALSE} otherwise (including
when \code{x} is not numeric).
}
\description{
Heuristically tests whether \code{x} holds non-negative integer (count-like) values, as
opposed to already-transformed (relative abundance, log, etc.) data. For large
inputs, only a subsample of values is checked by default (see \code{consider_small_part}).
}
\examples{
is_count_like(matrix(1:6, nrow = 2))
is_count_like(matrix(c(0.1, 0.2, 0.3, 0.4), nrow = 2))
is_count_like(matrix(c(1, NA, 3, 4), nrow = 2), allow_na = FALSE)

}
