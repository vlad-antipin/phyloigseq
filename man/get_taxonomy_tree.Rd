% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_tree.R
\name{get_taxonomy_tree}
\alias{get_taxonomy_tree}
\title{Get a phylogenetic tree from a taxonomy table}
\usage{
get_taxonomy_tree(
  ps,
  method = c("hierarchy", "rankwise", "longest_prefix"),
  rank_weight_base = 1.1
)
}
\arguments{
\item{ps}{A phyloseq object with a taxonomy table.}

\item{method}{\code{"hierarchy"} (default) builds the tree directly from the
taxonomy's rank structure via \code{\link[=build_taxonomy_tree_hierarchy]{build_taxonomy_tree_hierarchy()}} -- no
pairwise distance matrix, no \code{hclust()}, scales to arbitrarily large
datasets. \code{"rankwise"} and \code{"longest_prefix"} go through an n x n
distance matrix (\code{\link[=build_taxonomy_distance_rankwise]{build_taxonomy_distance_rankwise()}} /
\code{\link[=build_taxonomy_distance_longest_prefix]{build_taxonomy_distance_longest_prefix()}}) + UPGMA instead; they are
kept for reference/small datasets but do not scale, and \code{"rankwise"}
raises an informative error rather than crashing if the dataset is too
large for its sparse-matrix approach.}

\item{rank_weight_base}{Controls how quickly rank weights decay from
Kingdom (highest) to the finest rank (lowest): weight at depth \code{d}
(from the deepest) is \code{rank_weight_base^d}, normalized to sum to 1. A
base close to 1 weights all ranks near-evenly; a larger base makes
coarse-rank differences dominate the distance. Passed through to
whichever \code{method} is selected.}
}
\value{
An object of class \code{phylo}.
}
\description{
Dispatches to one of the taxonomy-based tree-building methods and
returns a tree whose tip labels match \code{taxa_names(ps)}.
}
