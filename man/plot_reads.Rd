% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{plot_reads}
\alias{plot_reads}
\title{Plot Distributions of Total Read Counts by Sample and by Taxon}
\usage{
plot_reads(physeq, min_sample_sum = NA, min_taxa_sum = NA)
}
\arguments{
\item{physeq}{A \code{phyloseq} object.}

\item{min_sample_sum}{Numeric threshold to draw as a vertical reference
line on the sample-level histogram. \code{NA} (default) or \code{NULL} omits it.}

\item{min_taxa_sum}{Numeric threshold to draw as a vertical reference line
on the taxon-level histogram. \code{NA} (default) or \code{NULL} omits it.}
}
\value{
A \code{ggplot} object faceted by level (\code{"Sample"}/\code{"Taxon"}). \code{NULL}
if \code{physeq} is not a \code{phyloseq} object.
}
\description{
Histograms of per-sample and per-taxon total read counts, faceted side by
side, with an optional vertical reference line marking a filtering
threshold on each facet.
}
\examples{
data(ps_16s_refinement)
plot_reads(ps_16s_refinement, min_sample_sum = 100, min_taxa_sum = 5)
}
