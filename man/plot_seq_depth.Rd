% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_seq_depth}
\alias{plot_seq_depth}
\title{Plot Sequencing Depth}
\usage{
plot_seq_depth(ps, type = c("bar", "box"), x_var = NULL, facet_var = NULL)
}
\arguments{
\item{ps}{A \code{\link[phyloseq]{phyloseq}} object with an OTU table and
sample data.}

\item{type}{Character. Plot type: \code{"bar"} (default) for a bar chart
ordered by sample name, or \code{"box"} for a boxplot grouped by a
metadata variable.}

\item{x_var}{Character. Name of a sample-data column to use as the x-axis
grouping variable. Required when \code{type = "box"}, ignored otherwise.}

\item{facet_var}{Character or \code{NULL}. Name of a sample-data column used
to facet the plot with \code{\link[ggplot2]{facet_wrap}}. Only applied
when \code{type = "box"}. Default \code{NULL} (no faceting).}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Visualises per-sample sequencing depth from a \code{\link[phyloseq]{phyloseq}}
object as either a bar chart (one bar per sample) or a grouped boxplot with
jittered points.
}
\examples{
data(ps_16s_refinement)
plot_seq_depth(ps_16s_refinement)
plot_seq_depth(ps_16s_refinement, type = "box", x_var = "Protocol")

}
\seealso{
\code{\link{plot_rarefaction}}
}
