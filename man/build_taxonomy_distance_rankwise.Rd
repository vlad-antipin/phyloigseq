% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_tree.R
\name{build_taxonomy_distance_rankwise}
\alias{build_taxonomy_distance_rankwise}
\title{Taxonomic distance from rankwise shared weight}
\usage{
build_taxonomy_distance_rankwise(
  phyloseq_object,
  rank_weights = NULL,
  rank_weight_base = 1.1
)
}
\arguments{
\item{phyloseq_object}{A phyloseq object with a taxonomy table.}

\item{rank_weights}{Optional numeric vector of weights, one per rank
(including the appended taxon-name rank), highest rank first. Defaults
to \code{rank_weight_base^((number_of_ranks - 1):0)}, normalized to sum to 1.}

\item{rank_weight_base}{Base of the default exponential rank-weight
decay (see \code{\link[=get_taxonomy_tree]{get_taxonomy_tree()}} for its effect). Ignored if
\code{rank_weights} is supplied.}
}
\value{
A \code{dist} object with one entry per taxon pair.
}
\description{
Computes a heuristic taxonomic distance between taxa by comparing them
rank-by-rank (Kingdom, Phylum, ..., plus an implicit finest rank made of
the taxon names) and summing the weight of every rank where they match,
regardless of whether shallower ranks also matched. Higher ranks get
larger weights, since differences at broad taxonomic levels represent
larger taxonomic divergence.
}
\details{
\code{distance = 1 - similarity}, where
\verb{similarity = sum(weight of ranks where taxonomy is identical)}. As with
\code{\link[=build_taxonomy_distance_longest_prefix]{build_taxonomy_distance_longest_prefix()}}, appending taxon names as
an implicit finest rank guarantees distance is never exactly 0 between
two different taxa. After \code{\link[=make_unique_taxa_table]{make_unique_taxa_table()}} has disambiguated
repeated labels across lineages, this produces exactly the same result
as \code{\link[=build_taxonomy_distance_longest_prefix]{build_taxonomy_distance_longest_prefix()}} (a rankwise match can
never occur past a real point of divergence), but is vectorized via a
sparse one-hot encoding + crossprod rather than an explicit double loop.

Requires an n x n similarity matrix, so it does not scale to large
datasets -- use \code{\link[=build_taxonomy_tree_hierarchy]{build_taxonomy_tree_hierarchy()}} instead for those. This
function raises an informative error (rather than crashing) if the
sparse crossprod would overflow.
}
\examples{
data(ps_16s_refinement)
d <- build_taxonomy_distance_rankwise(ps_16s_refinement)
class(d)
as.matrix(d)[1:3, 1:3]
}
