% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_reads}
\alias{filter_reads}
\title{Filter Taxa and Samples by Minimum Read Sum}
\usage{
filter_reads(physeq, min_sample_sum = 100, min_taxa_sum = 2, taxa_first = TRUE)
}
\arguments{
\item{physeq}{A \code{phyloseq} object.}

\item{min_sample_sum}{Numeric threshold; samples with total reads below
this value are removed. \code{NULL} skips read-count filtering entirely
(\code{physeq} is returned unfiltered, only reoriented/re-sparsified).}

\item{min_taxa_sum}{Numeric threshold; taxa with total reads below this
value are removed. \code{NULL} skips read-count filtering entirely, same as
\code{min_sample_sum = NULL}.}

\item{taxa_first}{Logical. If \code{TRUE} (default), taxa are filtered before
samples, so sample sums are computed on the taxon-filtered subset; if
\code{FALSE}, samples are filtered first and taxon sums are computed on the
sample-filtered subset.}
}
\value{
The filtered \code{phyloseq} object. \code{NULL} if \code{physeq} is not a
\code{phyloseq} object, or if filtering would remove every taxon or every
sample (a \code{warning()} is issued in that case naming which side emptied
out).
}
\description{
Removes taxa with fewer than \code{min_taxa_sum} total reads and samples with
fewer than \code{min_sample_sum} total reads. Filtering order matters, since
each side's sums are recomputed on whatever subset the other side's filter
has already produced (see \code{taxa_first}).
}
\examples{
data(ps_16s_refinement)
filtered <- filter_reads(
  ps_16s_refinement,
  min_sample_sum = 100,
  min_taxa_sum = 5
)
phyloseq::ntaxa(filtered)
phyloseq::nsamples(filtered)
}
