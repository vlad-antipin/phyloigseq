% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igseq_data.R
\name{resolve_ig_freqs}
\alias{resolve_ig_freqs}
\title{Resolve the Per-Fraction Ig+ Frequencies of One Sample}
\usage{
resolve_ig_freqs(
  sam_metadata_df,
  fraction_id_name,
  ig_freq_name,
  ig_freq_units,
  ig_freq_layout,
  positive_fraction_name,
  first_negative_fraction_name = NULL,
  second_negative_fraction_name = NULL,
  presorting_fraction_name = NULL,
  sample_id = NA_character_
)
}
\arguments{
\item{sam_metadata_df}{One sample's \code{sample_data} rows (one per fraction), still carrying the
\code{fraction_id_name} column.}

\item{fraction_id_name}{Name of the fraction column in \code{sam_metadata_df}.}

\item{ig_freq_name}{Name of the Ig+ frequency column, or \code{NULL} (all four come back \code{NA}).}

\item{ig_freq_units, ig_freq_layout}{See \code{\link[=getPhyloIgSeq]{getPhyloIgSeq()}}.}

\item{positive_fraction_name}{The \emph{single} positive fraction currently being scored, so that
\code{pos_ig_freq} follows the positive fraction of the synthetic sample it belongs to.}

\item{first_negative_fraction_name, second_negative_fraction_name, presorting_fraction_name}{The remaining fraction names, any of which may be \code{NULL}.}

\item{sample_id}{Used to identify the sample in warnings.}
}
\value{
A one-row data frame with numeric columns \code{presort_ig_freq}, \code{pos_ig_freq},
\code{neg1_ig_freq} and \code{neg2_ig_freq}.
}
\description{
Reads \code{getPhyloIgSeq()}'s \code{ig_freq_name} column out of one sample's \code{sample_data}
rows and returns the Ig+ frequency of each sort fraction, all validated the same way
\code{\link[=getPhyloIgSeq]{getPhyloIgSeq()}} validates them internally.
}
\details{
Exported because callers that bypass \code{\link[=getPhyloIgSeq]{getPhyloIgSeq()}} still need the same numbers on
the same terms: the companion Shiny app's MA-plot preview goes straight from
\code{\link[=group_sorted_samples]{group_sorted_samples()}} to \code{\link[=get_ma_coordinates]{get_ma_coordinates()}}, and the purity-corrected change
axis needs \code{pos_ig_freq}/\code{neg1_ig_freq} resolved with the same unit conversion and
\verb{[0, 1]} range checking rather than re-implemented.

The two layouts differ in what the column is taken to mean:
\describe{
\item{\code{"wide"}}{One Ig+ frequency per biological sample, repeated on (or recorded on only
one of) its fraction rows -- the historical layout. Only the pre-sort frequency P(Ig+) is
recoverable; the in-fraction frequencies are \code{NA}, since a single overall P(Ig+) says
nothing about how pure either sorted fraction turned out to be.}
\item{\code{"long"}}{The frequency is fraction-specific: each fraction's row records the Ig+
frequency measured \emph{in that fraction}. This yields all four values -- the pre-sort P(Ig+)
plus the "positive fraction purity" and "negative fraction impurity" needed by the
\verb{purity_corrected_*} scores.}
}
}
\examples{
data(ps_igseq)
metadata <- as(phyloseq::sample_data(ps_igseq), "data.frame")
resolve_ig_freqs(
  sam_metadata_df = metadata[metadata$sample_id == "sample_1", ],
  fraction_id_name = "sorting_fraction",
  ig_freq_name = "ig_pheno",
  ig_freq_units = "frequency",
  ig_freq_layout = "wide",
  positive_fraction_name = "pos",
  first_negative_fraction_name = "neg1"
)

}
