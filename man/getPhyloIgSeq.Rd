% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\name{getPhyloIgSeq}
\alias{getPhyloIgSeq}
\title{Compute Ig Scores from a Phyloseq Object}
\usage{
getPhyloIgSeq(
  physeq,
  taxrank = NULL,
  sample_id_name,
  sample_ids = NULL,
  fraction_id_name,
  rarefy_by_sample = TRUE,
  transform_by_sample = "identity",
  positive_fraction_name = "pos",
  first_negative_fraction_name = NULL,
  second_negative_fraction_name = NULL,
  presorting_fraction_name = NULL,
  ig_freq_name = NULL,
  ig_freq_units = c("frequency", "percent"),
  ig_freq_layout = c("wide", "long"),
  zero_treatment = "no_zero",
  window_size = 50,
  empirical_null_distribution = TRUE,
  confidence_levels = NULL,
  scores = IG_SCORES,
  taxon_id_source = c("sequential", "original")
)
}
\arguments{
\item{physeq}{A \code{phyloseq} object containing raw count data.}

\item{taxrank}{Character or NULL. Taxonomic rank to agglomerate to before computing scores.}

\item{sample_id_name}{Name of the column identifying unique samples.}

\item{sample_ids}{Optional. A character vector of sample IDs to subset.}

\item{fraction_id_name}{Name of the column indicating fraction (e.g., pos, neg).}

\item{rarefy_by_sample}{Logical. Rarefy read counts across fractions within each sample.}

\item{transform_by_sample}{Transformation method (e.g., "identity", "log").}

\item{positive_fraction_name}{Name of the positive fraction. Usually a single value, but can
be a character vector of several positive fraction values sharing the same negative/pre-sort
fraction(s) below (e.g. multiple phage-display sort rounds/output pools sequenced against one
shared negative/whole-community pool for the same samples). When more than one is given:
\itemize{
\item each (sample, positive fraction) combination becomes its own row, keyed by a synthetic
\code{sample_id} of the form \code{"<original sample_id>_<positive fraction>"};
\item the negative/pre-sort fraction(s) are rarefied \emph{once}, jointly across every
positive fraction of a sample, so they're compared against each positive fraction at the
same common depth (rather than independently re-rarefied per positive fraction, which
would add spurious noise between otherwise-comparable positive fractions);
\item \code{sample_data} gains two columns, \code{original_sample_id} and \code{positive_fraction_name},
recording which original sample and positive fraction each row came from;
\item the raw positive-fraction abundance column (normally named after the fraction's own
value, e.g. \code{"pos"}) is instead named \code{positive_fraction_abundance} and is always
populated, holding each row's own positive fraction's abundance.
}
With a single positive fraction (the default/common case), output is unchanged from previous
versions: no suffix, no extra columns, original fraction-named abundance column.

Rows from the same original sample across different positive fractions are \strong{not}
independent replicates -- they share the same underlying biology and the same negative/pre-sort
fraction.}

\item{first_negative_fraction_name}{Name of the first negative fraction, or \code{NULL}
to run without a negative fraction. In that case, \code{presorting_fraction_name} and
\code{ig_freq_name} must both be supplied instead, and only \code{"prob_index"} (of \code{scores})
can be computed — every other score, and \code{slide_z}'s MA-plot geometry, is a
positive-vs-negative comparison and requires a negative fraction.}

\item{second_negative_fraction_name}{Optional. Name of a second negative fraction.}

\item{presorting_fraction_name}{Optional. Name of the presorting fraction.}

\item{ig_freq_name}{Optional. Column name (in \code{physeq}'s \code{sample_data}) for the
Ig+ frequency phenotype, if precomputed. How it is read depends on \code{ig_freq_layout}.}

\item{ig_freq_units}{Units \code{ig_freq_name}'s values are recorded in: \code{"frequency"}
(default, already a probability in \verb{[0, 1]}) or \code{"percent"} (\verb{[0, 100]}, divided by
100 before use). After conversion, a value outside \verb{[0, 1]} for a given sample is
treated as \code{NA} for that sample (with a \code{warning()}) rather than fed into
\code{compute_ig_score()} — matching this function's general "skip what can't be
computed, don't abort the batch" behavior (see \code{\link[=get_slide_z]{get_slide_z()}}).}

\item{ig_freq_layout}{What one \code{ig_freq_name} value means:
\describe{
\item{\code{"wide"} (default)}{One Ig+ frequency per biological \emph{sample}, i.e. the same value
repeated on each of its fraction rows — the historical layout. Only the overall
P(Ig+) is recoverable, which is all \code{"prob_index"}/\code{"prob_ratio"} need. A column that
is \emph{not} constant across a sample's fractions can't be used and produces a \code{warning()}
pointing at \code{"long"}.}
\item{\code{"long"}}{The frequency is \emph{fraction-specific}: each fraction's row records the Ig+
frequency measured in that fraction. This yields the pre-sort P(Ig+) plus the
"positive fraction purity" and "negative fraction impurity" that the
\code{"purity_corrected_*"} scores require.}
}
Either way the resolved values are stored per sample in the returned object's \code{sample_data}
slot, as \code{presort_ig_freq}, \code{pos_ig_freq}, \code{neg1_ig_freq} and \code{neg2_ig_freq} (the last three
are \code{NA} under \code{"wide"}). With several \code{positive_fraction_name}s, \code{pos_ig_freq} follows each
row's own positive fraction.}

\item{zero_treatment}{How to handle zeros ("no_zero", "pseudocount", etc.).}

\item{window_size}{Integer. Window size for smoothing.}

\item{empirical_null_distribution}{Logical. Whether to estimate null distribution.}

\item{confidence_levels}{Optional. Confidence levels for scoring.}

\item{scores}{Vector of score names to compute, from \link{IG_SCORES} (the default is all of
them). A requested score whose inputs are not available -- a fraction it divides by, or
the Ig+ frequency it weights with -- is dropped with a \code{warning()} rather than returned
as an all-NA column, so \code{score_names} on the result lists what was actually computed.}

\item{taxon_id_source}{How to derive the \code{taxon_id} used throughout \code{ig_coating}/\code{tax_table}:
\code{"sequential"} (default) renumbers taxa as fresh sequential integers, recoverable via
\code{tax_table$taxon_name}; \code{"original"} uses \code{physeq}'s own (possibly \code{taxrank}-agglomerated,
and not necessarily ASV-level if \code{physeq} was already agglomerated upstream) taxa name
directly as \code{taxon_id}, matching the identifiers shown by \code{\link[=group_sorted_samples]{group_sorted_samples()}} when
called directly (e.g. for a single-sample preview).}
}
\value{
A \code{\link{PhyloIgSeq-class}} object -- always a single object, whether one or
several \code{positive_fraction_name} values were given (see that parameter's documentation
for the schema differences in the latter case). Its \code{ig_coating} slot holds one row per
taxon/sample with the requested \code{scores} as columns (also recorded in \code{score_names})
plus supporting fraction/diagnostic columns; see \code{\link{get_ig_score}} to retrieve a
single score without dealing with the rest of \code{ig_coating}.
}
\description{
This function computes various Ig scores based on sample fraction data.
}
\examples{
data(ps_igseq)
pis <- getPhyloIgSeq(
  physeq = ps_igseq,
  sample_ids = c("sample_1", "sample_2", "sample_3"),
  sample_id_name = "sample_id",
  fraction_id_name = "sorting_fraction",
  positive_fraction_name = "pos",
  first_negative_fraction_name = "neg1",
  second_negative_fraction_name = "neg2",
  scores = c("slide_z", "palm", "kau")
)
pis
get_ig_score(pis, score_name = "slide_z", sample_ids = "sample_1")

}
